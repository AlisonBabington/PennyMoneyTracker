<h2>Edit Transaction <%= @transaction.id %></h2>

<form action="/transactions/<%= @transaction.id %>" method= "POST">
  <label for="amount">Amount:</label>
  <input type="text" name="amount"
  id="amount-field" value="<%= @transaction.amount %>"
  />

  <label for="description">Description:</label>
  <input type="text" name="description"
  id ="description-field" value="<%= @transaction.description %>"
  />

  <!-- <label for="merchant_id">Merchant:</label>
  <select name="merchant_id" id="merchant_id">
    <% for merchant in @merchants %>
      <option value="<%= merchant.id %>"><%= merchant.name %></option>
    <% end %>
    <option selected="selected" value="<%= @transaction.merchant.id %>" > <%= @transaction.merchant.name %></option>
  </select>

  <label for="tag">Tag:</label>
  <select name="merchant_id" id="merchant_id">
    <% for merchant in @merchants %>
      <option value="<%= merchant.id %>"><%= merchant.name %></option>
    <% end %>
    <option selected="selected" value="<%= @transaction.merchant.id %>" > <%= @transaction.merchant.name %></option>
  </select> -->

  <input type='submit' value="Edit Transaction">
</form>
